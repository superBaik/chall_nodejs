extends templates/layout

block content
  h1=movie.title
  p= movie.synopsis
  if movie.genres
    ul
      each genre in movie.genres
        li=genre
  .video__info
    a(href=routes.editMovie(movie.id))
      button Edit video
